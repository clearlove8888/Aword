# Aword

Vue 3 + Vite 的基础背单词页面。

## 本地运行

```sh
npm install
npm run dev
```

## 构建

```sh
npm run build
npm run preview
```

包含 5 个示例单词、释义和例句显示、浏览器语音朗读、单词切换与熟悉度标记。标记仅保留在当前页面会话中，刷新后重置。语音可用性取决于浏览器和系统语音支持。
